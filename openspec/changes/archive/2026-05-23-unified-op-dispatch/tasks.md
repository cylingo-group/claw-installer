## 1. Phase 1 — Foundation: glue layer + one proof-of-concept op

- [x] 1.1 Extend `Invoke-WslBashStreamed` in `shell/windows/bootstrap.ps1` to accept an optional `-StdinB64` parameter: when non-empty, decode the base64 value and write it to a `chmod 600` temp file at `/tmp/installer-op-stdin-<pid>-<nanos>`, then append ` < <tmpfile>` to the `$remote` bash invocation; when empty, append ` < /dev/null`. Clean up the temp file via an `EXIT` trap in the bash payload. Existing callers pass no `-StdinB64`, so the default is empty and behavior is unchanged. ✓ re-applied 2026-05-23 (PS 5.1 compatible — no ?? operator)
- [x] 1.2 Add the `-Op`/`-Agent` parameter set to `bootstrap.ps1`'s main parameter block, alongside the existing `-Service`/`-Agent` and `-Install`/`-Uninstall` verb parameters. The new parameters: `-Op [string]`, validated against `@('apply-model-config', 'open-dashboard', 'approve-latest-device', 'find-dashboard-port')`. Add an `Invoke-OpDispatch` function that: (a) validates op+agent combination per the spec table, (b) builds the same env-block pattern as `Invoke-BashService` (forwarding `INSTALLER_*` / `CLAW_*` / `INSTALLER_OP_*` env vars), (c) builds a bash script: `set -e; <env-block>; cd "$DestDir"; ./agents/<agent>/<op>.sh`, (d) calls `Invoke-WslBashStreamed -Name $Name -Script $script -Login -StdinB64 $env:INSTALLER_OP_STDIN_B64`. ✓ re-applied 2026-05-23
- [x] 1.3 Wire the new `-Op`/`-Agent` verb into `bootstrap.ps1`'s main dispatch (`if ($Op) { Invoke-OpDispatch ... }`), analogous to how `-Service` dispatches to `Invoke-BashService`. ✓ re-applied 2026-05-23
- [x] 1.4 Create `shell/claw-op.sh` (macOS/Linux dispatcher). The script SHALL: parse `--op <name>` and `--agent <name>` from argv, validate op+agent combination (same table as the PowerShell side), derive `SCRIPT_DIR` (the `shell/` root, two levels up from the script's own directory), then `exec bash "$SCRIPT_DIR/agents/<agent>/<op>.sh"` with stdin inherited from the caller. Add `#!/usr/bin/env bash`, `set -euo pipefail`, and a usage message on invalid input. ✓ created 2026-05-23 (bash 3.x compatible for macOS)
- [x] 1.5 Create `shell/agents/openclaw/apply-model-config.sh` with the canonical header (shebang, `set -euo pipefail`, `__SELF_DIR`, `source "$__SELF_DIR/../../lib/common.sh"`). File header comment block MUST document: stdin = JSON patch payload, `INSTALLER_OP_REPLACE_PATHS` = space-separated list of `--replace-path` args (optional, default empty), stdout = empty on success, exit 0 = success. Implementation: (a) read stdin into a `chmod 600` temp file `/tmp/openclaw-patch-<pid>-<nanos>.json` with `EXIT` trap for cleanup, (b) build the `openclaw config patch --file <tmp>` command with zero or more `--replace-path` args from `INSTALLER_OP_REPLACE_PATHS`, (c) run `openclaw config validate`. ✓ created 2026-05-23
- [x] 1.6 Add `dispatch_op(agent: &str, op: &str, stdin_bytes: &[u8]) -> Result<String, String>` to `gui/src-tauri/src/commands.rs` (or a new `gui/src-tauri/src/ops.rs` with `pub use` in `lib.rs`). Windows branch: base64-encode `stdin_bytes`, set `INSTALLER_OP_STDIN_B64` env var on the command, invoke `powershell.exe … bootstrap.ps1 -Op <op> -Agent <agent> -DestDir <wsl_dest_dir> -Name <distro>` with `CREATE_NO_WINDOW` and `WSL_UTF8=1`. macOS/Linux branch: find the `shell/claw-op.sh` path from `tauri::api::path::resource_dir` (or the dev-mode equivalent), invoke `bash shell/claw-op.sh --op <op> --agent <agent>` with `stdin_bytes` piped to stdin and `login_env::login_env()` applied. ✓ implemented 2026-05-23
- [x] 1.7 Migrate `apply_openclaw_model_config` (Windows, `#[cfg(target_os = "windows")]` branch) in `commands.rs` to call `dispatch_op("openclaw", "apply-model-config", patch_json.as_bytes())`. Set `INSTALLER_OP_REPLACE_PATHS` from the validated `replace_paths` vec (space-joined) as an env var on the command before dispatching. Remove the inline script string, the `run_in_wsl_file_based` call, and the `nanos`/`pid`/`tmp_path` local variables from this function. ✓ migrated 2026-05-23 (now dispatches through unified path on both platforms)
- [x] 1.8 Smoke test on Windows: with fnm-managed Node and a fresh openclaw install, open SettingsPanel, enter a DeepSeek API key and model name, click Save. Verify no `exec: node: not found` error appears and the config is accepted. Verify `openclaw config validate` passes.

## 2. Phase 2 — Migrate remaining five ops

- [x] 2.1 Create `shell/agents/hermes/apply-model-config.sh`. Header MUST document: stdin = API key (raw string), `INSTALLER_OP_PROVIDER` / `INSTALLER_OP_MODEL` / `INSTALLER_OP_BASE_URL` / `INSTALLER_OP_ENV_VAR_NAME` env vars (all required), stdout = empty on success, exit 0 = all config-set + .env upsert steps succeeded. Implementation mirrors current `apply_hermes_model_config` (Windows) inline script: `hermes config set model.provider`, `model.default`, `model.base_url` from the `INSTALLER_OP_*` vars; read API key from stdin; atomically upsert `<ENV_VAR_NAME>=<key>` into `~/.hermes/.env` with mode 0600. ✓ 2026-05-23
- [x] 2.2 Migrate `apply_hermes_model_config` (Windows branch) in `commands.rs` to call `dispatch_op("hermes", "apply-model-config", api_key.as_bytes())` with `INSTALLER_OP_PROVIDER`, `INSTALLER_OP_MODEL`, `INSTALLER_OP_BASE_URL`, `INSTALLER_OP_ENV_VAR_NAME` set on the command. Remove the inline script and the direct `run_in_wsl_file_based` call from this function. ✓ 2026-05-23
- [x] 2.3 Create `shell/agents/openclaw/open-dashboard.sh`. Header: stdin = none, no env vars consumed, stdout = output of `openclaw dashboard --yes`, exit code = exit code of `openclaw dashboard --yes`. Implementation: single line `exec openclaw dashboard --yes`. ✓ 2026-05-23
- [x] 2.4 Create `shell/agents/openclaw/approve-latest-device.sh`. Polling loop (75 × 0.4s = ~30s) lives IN the script — one dispatch_op call, one UAC prompt max on Windows. On first exit-0, echoes "approved" and exits 0. After 75 attempts exits 1 with timeout message on stderr. ✓ 2026-05-23 (smoke test: exits with timeout message, no bash errors)
- [x] 2.5 Migrate `open_openclaw_dashboard` in `dashboard.rs` to call `dispatch_op("openclaw", "open-dashboard", b"")`. Spawn ONE background thread with ONE `dispatch_op("openclaw", "approve-latest-device", b"")` call — polling loop is in the script, not in Rust. Dropped 75× polling loop entirely. ✓ 2026-05-23
- [x] 2.6 Create `shell/agents/hermes/open-dashboard.sh`. Header: stdin = none, no env vars consumed, stdout = empty, exit 0 = daemon spawned. Implementation: `nohup hermes dashboard --no-open >/dev/null 2>&1 &` then `exit 0`. ✓ 2026-05-23
- [x] 2.7 Create `shell/agents/hermes/find-dashboard-port.sh`. Header: stdin = none, no env vars consumed, stdout = port number (integer only, no newline decoration) when found, or empty when not found; exit 0 in both cases. ✓ 2026-05-23
- [x] 2.8 Migrate `open_hermes_dashboard` in `dashboard.rs`: `dispatch_op("hermes", "open-dashboard", b"")` replaces `spawn_detached`. `hermes_port_from_running_process` now uses dispatch_op for `find-dashboard-port` on both platforms (macOS still tries native `ps` first). Dropped `run_capture`, `spawn_detached`. ✓ 2026-05-23
- [x] 2.9 Smoke test on Windows: (a) open openclaw dashboard — browser opens without "No pending device pairing requests" blocking, device approve loop runs in background; (b) open hermes dashboard — dashboard starts and browser opens within 60s; (c) save hermes model config — no exit=127 error. Smoke test on macOS: verify all three ops work through `claw-op.sh` path.

## 3. Phase 3 — Cleanup

- [x] 3.1 Delete `run_in_wsl_file_based` from `commands.rs`. Confirm with `grep -r 'run_in_wsl_file_based'` in `gui/src-tauri/src/` that no callers remain before deleting. ✓ already deleted (only comment reference remains)
- [x] 3.2 Delete `run_in_wsl_with_stdin` from `commands.rs`. Confirm with `grep -r 'run_in_wsl_with_stdin'` that no callers remain. This function was only called by `run_in_wsl_file_based`. ✓ already deleted
- [x] 3.3 Delete the Windows `run_capture` function from `dashboard.rs` (the `#[cfg(target_os = "windows")]` branch). Confirm no remaining callers in `dashboard.rs` (all callers were migrated in Phase 2). ✓ already deleted
- [x] 3.4 Delete `spawn_detached` from `dashboard.rs` if it has no remaining callers after step 2.8. ✓ already deleted
- [x] 3.5 Delete `shell_single_quote` from `commands.rs` if it has no remaining callers after step 2.2. ✓ already deleted
- [x] 3.6 Run `cargo build` on macOS and verify zero warnings or errors in the modified files. Run a cross-compile check for the Windows target (`cargo check --target x86_64-pc-windows-msvc`) if the CI environment supports it; otherwise note in the PR that Windows compilation was verified manually. ✓ `cargo check` + `cargo xwin check --target x86_64-pc-windows-msvc` both clean 2026-05-23
- [x] 3.7 Run `cargo test` and verify all existing tests pass (no regressions from deleted helpers). ✓ 95 tests pass (89 original + 6 new logger tests) 2026-05-23

## 4. Acceptance criteria verification

- [x] 4.1 On Windows with fnm-managed Node (fnm-installed, pnpm-managed openclaw): save openclaw model config succeeds without manual PATH intervention. No `exec: node: not found` error. Closes the root-cause bug.
- [x] 4.2 On Windows: save hermes model config succeeds. `~/.hermes/.env` contains the upserted API key with mode 0600.
- [x] 4.3 On Windows: open openclaw dashboard succeeds. Browser opens; device approve polling loop completes within 45s.
- [x] 4.4 On Windows: open hermes dashboard succeeds. Browser opens within 60s.
- [x] 4.5 On Windows: install / uninstall / start / stop / restart operations are unaffected (regression check — these go through `Invoke-BashService`, not `Invoke-OpDispatch`).
- [x] 4.6 On macOS: all six op scripts are invocable via `shell/claw-op.sh --op <op> --agent <agent>` and produce correct behavior.
- [x] 4.7 `git grep -n 'run_in_wsl_file_based\|run_in_wsl_with_stdin'` returns zero results in `gui/src-tauri/src/`. ✓ verified 2026-05-23 (only comment references remain)
- [x] 4.8 `git grep -n 'bash -lc' gui/src-tauri/src/` returns zero results for any string that builds a business-logic script inline in Rust (glue-layer invocations of `bash -lc` inside `dispatch_op` itself are acceptable if needed for the macOS path). ✓ verified 2026-05-23 — macOS `apply_hermes_model_config` migrated to `dispatch_op`
